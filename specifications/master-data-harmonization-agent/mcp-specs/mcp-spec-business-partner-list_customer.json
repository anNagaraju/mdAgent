{
  "name": "list_customer",
  "title": "list_A_Customer",
  "mcpServerOrdId": "sap.mcpbuilder:apiResource:business_partner_mcp_demo:v1",
  "description": "Retrieve a list of entities from the A_Customer collection in the api_business_partner service. Supports OData query options for filtering, sorting, pagination, and field selection.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [],
    "properties": {
      "filter": {"title": "filter", "description": "OData $filter expression", "type": "string"},
      "select": {"title": "select", "description": "Comma-separated property names", "type": "string"},
      "orderby": {"title": "orderby", "type": "string"},
      "top": {"title": "top", "type": "integer", "minimum": 0},
      "skip": {"title": "skip", "type": "integer", "minimum": 0},
      "expand": {"title": "expand", "type": "string"},
      "inlinecount": {"title": "inlinecount", "type": "string", "enum": ["allpages", "none"]}
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "Customer": {"maxLength": 10, "type": "string"},
            "AuthorizationGroup": {"maxLength": 4, "type": ["string", "null"]},
            "BillingIsBlockedForCustomer": {"maxLength": 2, "type": ["string", "null"]},
            "CreationDate": {"type": ["string", "null"]},
            "CustomerAccountGroup": {"maxLength": 4, "type": ["string", "null"]},
            "CustomerFullName": {"maxLength": 220, "type": ["string", "null"]},
            "CustomerName": {"maxLength": 81, "type": ["string", "null"]},
            "IsMarkedForDeletion": {"type": ["boolean", "null"]}
          }
        }
      }
    }
  }
}
